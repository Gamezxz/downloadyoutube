# YouTube MP3 Downloader

เว็บไซต์สำหรับแปลงและดาวน์โหลด YouTube เป็น MP3 คุณภาพสูง 320kbps

## 🚀 วิธีใช้งาน

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. สร้างไฟล์ Cookies (สำคัญมาก!)

เนื่องจาก YouTube มีการป้องกันการดาวน์โหลด คุณต้อง export cookies จาก browser:

#### ขั้นตอน:

1. **ติดตั้ง Browser Extension**
   - Chrome: [Get cookies.txt LOCALLY](https://chrome.google.com/webstore/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc)
   - Firefox: [cookies.txt](https://addons.mozilla.org/en-US/firefox/addon/cookies-txt/)

2. **Login เข้า YouTube**
   - ไปที่ https://www.youtube.com
   - Login ด้วย Google Account ของคุณ

3. **Export Cookies**
   - คลิกที่ icon ของ extension
   - เลือก "Export" หรือ "Current Site" 
   - บันทึกไฟล์เป็น `cookies.txt`

4. **วางไฟล์ในโปรเจค**
   - วางไฟล์ `cookies.txt` ไว้ในโฟลเดอร์หลักของโปรเจค (ข้าง package.json)

### 3. รันเซิร์ฟเวอร์
```bash
npm run dev
```

### 4. เปิดเว็บไซต์
เปิด browser ไปที่ http://localhost:3000

## 📁 โครงสร้างไฟล์

```
dlyt/
├── package.json
├── server.js
├── cookies.txt        <- ไฟล์ cookies ของคุณ (ต้องสร้างเอง)
├── README.md
└── public/
    ├── index.html
    ├── styles.css
    └── app.js
```

## ⚠️ หมายเหตุสำคัญ

- **Cookies หมดอายุ**: Cookies จาก YouTube มักหมดอายุหลังจากไม่กี่สัปดาห์ ถ้าเจอ error ให้ export cookies ใหม่
- **ใช้ส่วนตัวเท่านั้น**: ใช้สำหรับดาวน์โหลดเนื้อหาที่มีลิขสิทธิ์ถูกต้องเท่านั้น
- **ไม่แชร์ cookies**: ไฟล์ cookies.txt มีข้อมูล session ของคุณ อย่าแชร์กับผู้อื่น

## 🔧 API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/info?url=...` | ดึงข้อมูลวิดีโอ |
| GET | `/api/download?url=...` | ดาวน์โหลดเป็น MP3 |
| GET | `/api/status` | เช็คสถานะ cookies |
| POST | `/api/reload-cookies` | โหลด cookies ใหม่ |

## 🐛 แก้ไขปัญหา

### Error: "YouTube requires authentication"
→ สร้างไฟล์ cookies.txt ตามขั้นตอนด้านบน

### Error: "Status code: 403"
→ Cookies หมดอายุ ให้ export ใหม่

### Video ไม่โหลด
→ ตรวจสอบว่า video นั้นไม่ใช่ private หรือ age-restricted
